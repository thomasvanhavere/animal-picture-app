# =============================================================================
# .dockerignore : files that are NOT sent to Docker when building the image
#
# The Dockerfile installs and builds everything fresh, so installed packages,
# earlier build output, tests and local settings are left out.
# =============================================================================
node_modules
dist
coverage
test
*.log
.env
.env.*
